# Secrets — never commit
fastlane/.env
*.p8
*.p12
*.mobileprovision
*.cer

# Xcode / build
DerivedData/
build/
*.xcarchive
*.ipa
*.dSYM.zip
*.dSYM
xcuserdata/

# XcodeGen output (regenerated from project.yml)
Naru.xcodeproj/

# fastlane artifacts
fastlane/report.xml
fastlane/README.md
fastlane/test_output/

# macOS
.DS_Store
*.certSigningRequest
.gstack/

# Claude Code local state (worktrees, session artifacts)
.claude/worktrees/
context-log.md

# Docs kept local (IP) — README stays public
*.md
!/README.md

# Third-party skills pulled in by `npx skills add` (tooling, not project code)
.agents/
.claude/skills/*
!.claude/skills/animation-vocabulary/
!.claude/skills/emil-design-eng/
!.claude/skills/review-animations/
